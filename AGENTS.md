# 地獄廻線 プロジェクト実装方針

- このプロジェクトは「地獄廻線・七獄パック統一カードゲーム版」である。
- 旧版の駅別ミニゲームは完全廃止とし、再実装しない。
- 旧版仕様を推測・復元・再利用しない。
- 初期パックは7種類、各5枚、合計35枚とする。
- 参加者はGM1人＋PL7人とし、GMをプレイヤーの人数・順位・カード処理に含めない。
- PLの初期HPと最大HPは15とする。
- 各駅のターン数は `StationDefinition.turnCount` で管理し、処理や画面へハードコードしない。
- 未確定仕様を推測で補完しない。未確定箇所は `TODO` または明示的な設定値として残す。
- 秘密情報は権限のないクライアントへ送信しない。画面上で隠すだけの実装にしない。
- ゲーム状態の正本はサーバー側とし、ローカルストレージだけを正本にしない。
- カード効果、駅固有效果、ショップ商品は可能な限りデータ定義し、個別画面や条件分岐へのハードコードを避ける。
- ココフォリア上の路線図をWeb側から自動操作しない。WebはGMによる判定と進行を補助する。
- 作業は `unified-card-game` ブランチで行う。

